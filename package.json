{
  "name": "virtuasoc",
  "private": true,
  "version": "0.1.0",
  "description": "VirtuaSOC — Cloud SaaS Virtual Intelligence GSOC",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "integrations/*"
  ],
  "scripts": {
    "dev": "echo \"Use: npm -w @virtuasoc/api run dev / npm -w @virtuasoc/worker run dev / npm -w @virtuasoc/web run dev\"",
    "build": "npm --workspaces run build",
    "test": "npm --workspaces run test",
    "lint": "npm --workspaces run lint"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "cross-env": "^7.0.3",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
