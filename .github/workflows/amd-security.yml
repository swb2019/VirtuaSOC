# AMD-KIT wrapper (generated)
name: AMD Security
on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read
  security-events: write

jobs:
  security:
    uses: swb2019/amd-kit/.github/workflows/amd-security.yml@v1.5.4
    with:
      enable_codeql: true
      enable_dependency_review: false
      enable_gitleaks: false
      codeql_languages: "javascript-typescript"
    secrets: inherit