// Worker will be rebuilt in later steps (multi-tenant scheduling, ingest, drafting, distribution).
// This stub exists so the workspace builds while we incrementally rebuild the SaaS system.
console.log("[worker] stub");
