# intel-standards Module

Canonical intelligence analysis primitives (source reliability, analytic confidence, risk matrices, and action items) used across VirtuaSOC pipelines. Helpers are intentionally framework-agnostic and fully deterministic so they can run inside workers, pipelines, or renderers without additional dependencies.
