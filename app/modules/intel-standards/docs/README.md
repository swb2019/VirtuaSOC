# intel-standards

Canonical primitives for intelligence products:

- AF source reliability scale (`A`-`F`) + helper to render descriptions.
- Confidence levels (`high`, `moderate`, `low`).
- 5x5 likelihood/impact risk matrix with derived score & qualitative band.
- Action item helper enforcing owner + ISO deadline requirements.

All exports are pure TypeScript utilities; they do not hit external services.
