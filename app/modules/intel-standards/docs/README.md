# intel-standards

Domain primitives for intelligence production:

- Source reliability scale (A–F) + helper to fetch AF-standard descriptions.
- Analytic confidence levels and ordering helpers.
- A deterministic 5x5 risk matrix + scoring helper.
- Action item constructor + overdue checks.

All utilities are pure TypeScript and require no external services. Use them anywhere you need to normalize product outputs.
